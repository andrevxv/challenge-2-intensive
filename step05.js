let boolean = true

let isBoleanVar = typeof boolean === 'boolean' ? 'It is a boolean' : 'It is not a boolean'

function isBoolean(value){
    return typeof value === "boolean" ? 'It is a boolean' : 'It is not a boolean'
}

alert(isBoolean(boolean))

// console.log(isBoolean(boolean))
// console.log(typeof boolean)


